name: ci
on: [push]
jobs:
  build:
    name: build
    runs-on: macos-11
    steps:
    - run: ls
    - run: git clone https://github.com/chrismaltby/gb-studio.git
    - working-directory: gb-studio
      run: yarn
    - working-directory: gb-studio
      run: npm run make:cli 
    - working-directory: gb-studio
      run: yarn link
    - run : |
        sudo gb-studio-cli -v 
     
    